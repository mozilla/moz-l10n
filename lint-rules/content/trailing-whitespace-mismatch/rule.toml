name = "trailing-whitespace-mismatch"
family = "content"
summary = "Source and translation must agree on a trailing whitespace."
default_severity = "error"
enabled_by_default = true
fixable = true
formats = ["gettext"]
tags = ["recommended", "correctness"]
since = "0.1.0"
